spring:
  application:
    name: store-api
  jpa:
    open-in-view: true
    show-sql: true
    properties:
      hibernate.show_sql: true
      use_sql_comments: true
      format_sql: true
  flyway:
    enabled: true
    check-location: true
    locations: classpath:db/migration
    schemas: store_data
    table: schema_version



server:
  port: 8081
  servlet:
    context-path: /store-api


cox:
  store:
    data:
      spring:
        datasource:
          url: jdbc:mysql://localhost:3306/store_data?autoReconnect=true&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=UTC
          port: 3306
          username: root
          password: akhilesh
          schema: store_data
          type: com.zaxxer.hikari.HikariDataSource
          driver-class-name: com.mysql.cj.jdbc.Driver
      hibernate:
        show_sql: false
